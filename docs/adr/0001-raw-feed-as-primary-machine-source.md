# Use GitHub Raw Feed as the primary machine source

Daily AGI Radar will publish both GitHub raw files and a GitHub Pages site, but command-line and agent consumers will use the raw files as the primary machine-readable source. Raw files keep URLs directly tied to repository contents and commits, while Pages remains a human browsing surface and fallback mirror.
